{
  "name": "url-shortener-app",
  "version": "1.0.0",
  "description": "Runs frontend and backend concurrently",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm:start-backend\" \"npm:start-frontend\"",
    "start-backend": "cd \"Backend Test Submission\" && node index.js",
    "start-frontend": "cd \"Frontend Test Submission\\frontend\" && npm start",
    "install-all": "npm install && cd \"Backend Test Submission\" && npm install && cd .. && cd \"Frontend Test Submission\\frontend\" && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}